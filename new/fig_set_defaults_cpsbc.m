function fig_set_defaults_cpsbc
% Set figure defaults
% ------------------------------

cS = const_cpsbc(1);

% Set default line width
set(0, 'DefaultLineLineWidth', 2);

set(0, 'DefaultAxesColorOrder', cS.colorM);

end % eof
